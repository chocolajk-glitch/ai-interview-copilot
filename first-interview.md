# First Interview · 复盘学习计划

> 目标：把本次面试暴露的 3 个弱项补成"能应付追问"的水平，并配套可执行的"学习 → 检验"闭环。

---

## 一、本次面试暴露的弱项（按优先级）

| 优先级 | 弱项 | 暴露问题 | 真实原因 |
|---|---|---|---|
| P0 | 底层原理停留在"概念层"，下不到"机制层" | BGE 怎么把句子变向量、LangGraph 怎么用条件边、Chroma 用什么索引——只答到名词 | 没翻过源码/底层实现，只看了二手解读 |
| P1 | 设计决策缺乏"反问意识"，不主动暴露 trade-off | 三个 provider 一起创建是否浪费、引用溯源为什么放检索前——答完设计没补"我意识到 XX 代价" | 习惯正向叙述，缺少"质疑自己"的反射 |
| P2 | 概念与实现的边界模糊 | "LangGraph 封装好底层所以好用"——面试官想听"它封装了什么、解决了我手写时的什么痛点" | 工具/框架学的是"用法"不是"设计动机" |

---

## 二、学习流程（每个弱项都跑一遍：学 → 记 → 问 → 复盘）

### 阶段 A：底层原理补齐（P0，~5 天）

**A1. BGE / Embedding 原理（1.5 天）**
- [ ] **学**：读 BGE 官方 README 的 `Modeling` 章节 + HuggingFace 上 `BAAI/bge-small-zh-v1.5` 的 `modeling.py`（约 100 行，重点看 forward 函数）
- [ ] **记**：在笔记里手画一张图：`token → token embedding + position embedding → Transformer encoder → [CLS]向量 / mean pool → 768维句向量 → L2 归一化`
- [ ] **问自己并写下来答案**：
  1. BGE 用的是 BERT 类的双向 Transformer 还是 GPT 类的单向？为什么检索场景要双向？
  2. pooling 阶段为什么 [CLS] 和 mean pool 都能用？两者效果差多少？
  3. 为什么最后要做 L2 归一化？（提示：归一化后内积 = 余弦相似度）
  4. 微调时 BGE 用的是什么损失？（提示：对比学习 InfoNCE）
- [ ] **复盘**：能不看笔记把上面 4 题答出来 = 通过

**A2. LangGraph StateGraph 源码（1.5 天）**
- [ ] **学**：`pip install langgraph` 后读 `langgraph/graph/state.py`，重点看 `StateGraph.add_node` / `add_conditional_edges` 怎么实现
- [ ] **记**：手画状态流转图：`State (TypedDict) → Node 函数签名 (state) -> dict → Channel 合并 → 条件边函数返回下一个节点名`
- [ ] **问自己并写下来答案**：
  1. StateGraph 用什么数据结构存状态？（提示：TypedDict + Reducer 函数）
  2. 节点之间怎么传递状态？返回的是全量 state 还是增量？（提示：增量，通过 Reducer 合并）
  3. 条件边（conditional edge）的实现机制是什么？（提示：函数返回节点名字符串，框架查表跳转）
  4. 为什么 LangGraph 比 if-else 链强？本质区别是？（提示：状态可观测、可回放、支持循环和人机交互）
- [ ] **复盘**：能讲出"LangGraph 解决了我手写 if-else 时的 XX 痛点"= 通过

**A3. Chroma 索引结构（2 天）**
- [ ] **学**：读 Chroma 官方文档 "Architecture" 章节 + 看 `chromadb/utils/embedding_functions.py` 和 `chromadb/api/` 下的核心 API
- [ ] **记**：整理一张表：Chroma 默认用什么向量索引？（HNSW）为什么不用 FLAT？HNSW 的复杂度？什么规模下应该切到 IVF？
- [ ] **问自己并写下来答案**：
  1. Chroma 默认底层是 HNSW 吗？HNSW 全称是什么？复杂度多少？
  2. 10 万条以内数据，HNSW 暴力搜索也能扛，为什么还要建索引？（提示：常数项、内存）
  3. HNSW 的 `ef_construction` 和 `M` 两个参数分别控制什么？调大调小的代价？
  4. Chroma 持久化用的是什么存储？（提示：DuckDB + Parquet）
- [ ] **复盘**：能讲出"HNSW 是图索引，召回近似但快"= 通过

---

### 阶段 B：建立"反问意识"（P1，贯穿式训练）

**B1. 日常练习：每个设计决策都问自己"代价是什么"**
- [ ] **练**：本周每写一段代码 / 做一个技术决策，就在 PR 描述或 commit message 里**主动加一句"权衡"**
- [ ] **模板**：
  ```
  设计：XXX
  收益：XXX
  代价：XXX
  备选方案：XXX
  为什么没选备选：XXX
  ```
- [ ] **示例**（你项目里 provider 创建那段）：
  ```
  设计：工厂模式一次性创建三个 provider
  收益：调用时无需判断、简单
  代价：冷启动时三个模型连接全建起来，浪费资源 + 启动慢
  备选方案：懒加载 + 单例，用到时再 init
  为什么没选备选：项目规模小，三个 provider 初始化成本可接受
  ```

**B2. 面试模拟：每次答完一个设计，主动补"trade-off"**
- [ ] **练**：在镜子前或录音里答 3 遍"为什么用 LangGraph"，每遍都强制自己在结尾加一句"它的代价是 XX"
- [ ] **检验标准**：能自然地说出"这个方案的代价是学习成本高 / 性能开销大 / 复杂度上升"= 通过

---

### 阶段 C：概念 → 实现 → 设计动机的三层表达（P2，~3 天）

**C1. 找 3 个你最熟的框架/工具，写"设计动机卡"**
- [ ] **练**：选 3 个（建议 LangGraph / FastAPI / Redis），每张卡写 3 层：
  ```
  第 1 层（概念）：这是什么？解决什么问题？
  第 2 层（实现）：它底层用什么数据结构/算法？怎么实现的？
  第 3 层（动机）：作者为什么这样设计？解决了当时什么痛点？
  ```
- [ ] **示例**（FastAPI）：
  - 概念：高性能 Python Web 框架
  - 实现：基于 Starlette（ASGI）+ Pydantic（数据校验）+ 类型注解生成文档
  - 动机：Flask 时代没类型、没自动文档、async 不友好，作者想统一这三件事

**C2. 改造你的回答话术**
- [ ] **改前**："LangGraph 封装好底层逻辑，是 langchain 官方推荐"
- [ ] **改后**："LangGraph 把状态管理和条件跳转封装成图结构，**解决了我手写智能体时最痛的两个点**：①状态在多轮调用间丢失 ②条件分支嵌套深了以后 if-else 难维护。**代价是引入了一层抽象、学习成本比直写代码高**。"

---

## 三、检验机制：喂给下一个 AI 窗口的话术

下次你打开新窗口，直接把下面这段贴进去，让 AI 扮演追问式面试官检验你：

```
你现在是一位严格的 AI 面试官，专攻 AI 应用开发方向。
我接下来要复习 [BGE Embedding 原理 / LangGraph 源码 / Chroma 索引结构]
（按下面要复习的主题填一个）

规则：
1. 每次我问完一个概念后，你必须连续追问 3 层：
   第 1 层：这个是什么？解决什么问题？
   第 2 层：底层用什么数据结构/算法？实现机制是什么？
   第 3 层：为什么这样设计？有什么 trade-off？规模大了会怎么样？
2. 我答不上的地方不要直接给答案，先提示一个关键词让我自己想。
3. 我说"下一个"时才换题。
4. 全程计时，每个主题 15 分钟，超时强制进入下一主题。

我现在开始：
[第 1 题]：BGE 怎么把一个句子编码成一个 768 维向量？
```

---

## 四、时间表

| Day | 上午 (2h) | 下午 (2h) | 晚上 (1h) |
|---|---|---|---|
| D1 | A1 读 BGE 源码 | A1 记笔记 + 画图 | B1 写第一张"权衡卡" |
| D2 | A1 检验（贴话术问 AI） | A2 读 LangGraph 源码 | B1 写第二张"权衡卡" |
| D3 | A2 记笔记 + 画状态流转图 | A2 检验 | C1 写第一张"设计动机卡"（LangGraph） |
| D4 | A3 读 Chroma 文档 + 源码 | A3 记笔记 + 整理索引对比表 | C1 写第二张（FastAPI）+ 第三张（Redis） |
| D5 | A3 检验 | C2 用 AI 模拟面试 3 轮 | 整体复盘 + 重做本次面试的弱项题 |

---

## 五、通过标准（自己打分）

- [ ] BGE 那 4 题能不看笔记 90% 答对
- [ ] LangGraph 能讲出 StateGraph 的 3 个核心组件（State / Node / Edge）
- [ ] Chroma 能讲清楚 HNSW 是什么、为什么用它
- [ ] 写过的"权衡卡" ≥ 5 张
- [ ] 写过的"设计动机卡" ≥ 3 张
- [ ] 用 AI 模拟面试时，**主动说出"代价是 XX"** ≥ 3 次

**全部打勾 = 弱项已补齐，可以约下一次模拟面试。**
